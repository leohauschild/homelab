persistence:
  type: pvc
  enabled: true
  storageClassName: proxmox-csi-default
  accessModes:
    - ReadWriteOnce
  size: 5Gi

adminUser: admin
admin:
  existingSecret: "grafana-credentials"
  userKey: admin-user
  passwordKey: admin-password

datasources: []

sidecar:
  dashboards:
    enabled: true
